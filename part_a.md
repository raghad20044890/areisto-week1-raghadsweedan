# Part A — Quick Questions

1. b
2. b — `final` prevents the variable from being assigned a new value after its initialization.
3. b
4. b
5. b — `name` is nullable, so accessing `.length` directly is unsafe.
6. b
7. b
8. c
9. b — `Cat` inherits the members of `Animal` and can add or override behavior.
10. b

